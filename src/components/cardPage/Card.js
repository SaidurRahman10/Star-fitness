import React from "react";

const Card = (props) => {

    const {name, img , age , time, information} = props.star;
    const {handleAddToCart} = props;
  return (


 
    
   
   
  );
};

export default Card;
